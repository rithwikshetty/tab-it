# WS3 money and date bug fixes

## 2026-07-18 19:08 BST

Goal: fix GitHub issues #17, #25, #26, and #32 test-first. Preserve exact payment-ledger precision on currency changes, make expense-date display timezone-stable, keep user-edited settlement amounts across form changes, and normalize pulled monetary values to each currency's minor units.

## 2026-07-18 19:12 BST

Read `CLAUDE.md`, the domain glossary, the plain-writing guide, and the TDD and Supabase task guidance. The agreed test seams are pure form-decision helpers in `TabTests`, expense-date formatting in `TabTests`, and public `CurrencyCatalog` normalization in `TabCoreTests`. The current Supabase changelog has no breaking change relevant to decoding or locally normalizing pulled numeric values.

## 2026-07-18 19:13 BST

Issue #32 red/green: added the `CurrencyCatalog.normalizedAmount` contract with EUR, JPY, BHD, midpoint, and negative examples. The red run failed because the helper did not exist. Implemented `NSDecimalRound` with `.plain` at the catalog's fraction-digit scale; the focused eight-test `CurrencyCatalog` suite passed.

Added a real in-memory SwiftData merge test covering insert and update paths for expense totals, payment amounts, split amounts, and settlement amounts. Applied normalization at all eight `SyncMerge` assignments. The focused simulator test passed on `iPhone 17 Pro Max`. Direct shell simulator access is blocked by the workspace sandbox, so app tests run through the repository's Xcode build service with the same project, scheme, and simulator.

## 2026-07-18 19:18 BST

Issue #17 red/green: added pure payment-ledger tests for exact-entry currency re-rendering and the case where fractional JPY entries still sum to a valid total. The red run failed because `ExpensePaymentLedger` did not exist. Added the helper, wired exact committed payments into currency-switch normalization, and made ledger validity require both a matching sum and valid precision for every entry. Both focused simulator tests passed.

Tried to create the first requested logical Git commit, but this linked worktree stores its index at `/Users/rithwikshetty/Downloads/dev/tab/.git/worktrees/wt-ws3`, outside the writable sandbox. Git could not create `index.lock`. Terminal and Xcode UI control are also unavailable, so commits are deferred while implementation and validation continue.

## 2026-07-18 19:21 BST

Issue #25 red/green: added timezone cases for Pacific/Kiritimati, Pacific/Chatham, Pacific/Pago_Pago, and UTC. The first red run failed because stable display and picker-round-trip helpers did not exist. Added UTC-calendar expense-date formatting and a bridge that reconstructs the stored UTC calendar day in the device calendar; five focused tests passed.

Added a second failing expense-row test for UTC+14. Updated trip and friend timelines to derive expense grouping days from the stored calendar day while leaving settlement instants on the local calendar. Replaced expense detail and entry formatters with the UTC authority, reopened edited expenses on the correct picker day, and made exports use the stored `yyyy-MM-dd`. Seven focused expense-date and timeline tests passed.

## 2026-07-18 19:23 BST

Issue #26 red/green: added four pure draft-state tests for untouched, typed, typed-then-converted, and user-cleared amounts. The red run failed because the draft helper did not exist. Added `SettlementAmountDraft`, bound user input through it, preserved its edited flag during currency conversion, and gated pair/currency debt prefills on `shouldPrefill`. All four focused tests passed.

## 2026-07-18 19:25 BST

Review found that `UITextField` can report selection changes without changing its text. The settlement binding now ignores identical writes so merely focusing a programmatically prefilled field does not count as a user edit.

Full validation passed. `TabCore` ran 155 tests with no failures (`swift test --disable-sandbox` with module caches under `/tmp`, required because the workspace sandbox blocks SwiftPM's nested sandbox and user cache writes). The `TabTests` simulator suite ran 105 tests with no failures on `iPhone 17 Pro Max` through the Xcode build service using `Apps/Tab/Tab.xcodeproj`, scheme `Tab`, and `-only-testing:TabTests`. `git diff --check` also passed.

The remaining blocker is repository metadata access: this process can edit the worktree but cannot write the linked Git index in the parent repository, and the available local UI controls do not permit Terminal or Xcode. The source tree is validated but cannot be staged or committed from this sandbox.
